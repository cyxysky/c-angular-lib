import { WaterMarkDirectiveDirective } from './water-mark-directive.directive';

describe('WaterMarkDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new WaterMarkDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
