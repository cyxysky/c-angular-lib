import { SearchInElementDirective } from './search-in-element.directive';

describe('SearchInElementDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchInElementDirective();
    expect(directive).toBeTruthy();
  });
});
